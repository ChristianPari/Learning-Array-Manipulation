let firstLetter = "";
let name = "Christian";
firstLetter = name[0]; // can interchange the number to any index number within the string length, otherwise will be 'undefined'
// use " -1 " to find the last character in the string
// can then us " - x " with x being a 'number from last' to find the 'x from last character' in a string
console.log(firstLetter);